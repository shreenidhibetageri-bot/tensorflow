# tensorflow
Built a deep learning model using TensorFlow and CNN to classify images into multiple categories. The project includes data preprocessing, model training, evaluation, and prediction with real-time image input.
